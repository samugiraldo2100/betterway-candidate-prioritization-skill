# Local Text Extraction Workflow

Use this workflow to extract readable text from local job descriptions and candidate profiles without committing private files to the repository.

## Purpose

Candidate files may contain private information.

For that reason, source files and extracted text outputs must remain local and ignored by Git.

## Local Input Folder

Place local files in:

```text
data/input/
